


$(document).ready(function() {

    let height = window.screen.height;



    // setHeight(height);

    // $(window.screen.height).on('change', function() {
    //     console.log(window.screen.height);
    // });

});


function setHeight(size) {
    // // $('.card-home-search-hero').height(size);

    // let subHeight = $('#text-container').height();
    // console.log(subHeight);

    // const height = `${size}px`;
    // // const height = `${size - subHeight}px`;
    // console.log(height);

    // // document.querySelector('.card-home-search-hero').style.height = height;
    // document.querySelector('.card-img').style.height = height;
}